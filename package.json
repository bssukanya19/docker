FROM node:20

WORKDIR /app

COPY package*.json ./

RUN npm install

COPY . .

EXPOSE 5000

CMD ["npm", "start"]
ubuntu@ip-172-31-42-211:~/docker-compose-app/backend$ cat package.json
{
  "name": "docker-compose-backend",
  "version": "1.0.0",
  "description": "Backend API for Docker Compose application",
  "main": "server.js",
  "scripts": {
    "start": "node server.js"
  },
  "dependencies": {
    "express": "^4.18.2",
    "mysql2": "^3.9.8"
  }
}